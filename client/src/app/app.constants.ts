export const SERVER_API_URL = 'http://localhost:8080';

export enum COMMONs {
  REDIRECT_URL_PARAM = 'redirectUrl',
  SERVER_ERROR = 'Server error!',
  METAADATOK = 'meta_adatok.'
}

export enum URLs {
  LOGIN = 'bejelentkezes',
  ACCESS_DENIED = 'access-denied',
  OLDAL_NEM_ELERHETO = '404',
  ROOT = '/',
  FORBIDDEN = '403'
}
